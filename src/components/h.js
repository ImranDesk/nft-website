
  return (
    
      <div className="cards">
        {users.map((curElem) => {
          return (
            <div className="card">
              
            </div>
          );
        })}
      </div>
  
  );


export default UseEffectAPI;
